# Locations Index

Every page under `~/memory/locations/`, most important first — one file per
significant place in the user's life. Read the matching page whenever a
location comes up. See the skill's `references/layout.md`.

- **(Example) Home** — `~/memory/locations/home.md` — (one-line description;
  delete this example when adding real pages)
