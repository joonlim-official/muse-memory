# Trace Index

- **trace.md** — the append-only temporal log, newest first. Top = latest
  knowledge; deeper = history. Statuses: `new` / `updated` / `superseded` /
  `open` / `closed`. See the skill's `references/trace-conventions.md`.

## Open threads

- [ ] (open items live here until resolved, then become
      `[x] YYYY-MM-DD — how it resolved`, plus a `closed` trace entry)
